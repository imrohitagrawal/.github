{
  "name": "self-test-node-only",
  "private": true,
  "version": "1.0.0",
  "description": "WP1 self-test fixture: Node-only project (no Python manifest), calls the reusable workflow with default python-directory/node-directory so it exercises the root-detect Node path against a real, checked-in package.json rather than an asserted result. lint/typecheck/build each run a real check against real content (node --check, requiring package.json, requiring+executing smoke.test.js), not print-only stubs - round-1 review fixed lint/typecheck; round-2 review found build was left as the same kind of stub and fixed it too.",
  "scripts": {
    "lint": "node --check smoke.test.js",
    "typecheck": "node -e \"require('./package.json')\"",
    "test": "node --test",
    "build": "node -e \"require('./smoke.test.js'); console.log('fixture build: ok')\""
  }
}
